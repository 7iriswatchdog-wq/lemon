@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer localizer
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "TMS Case Report";
    Layout = "~/Views/Shared/AdminLTE/_Layout.cshtml";
}

@model AML.ViewModel.ViewModels.TransactionMonitor.TMSReportModel
<script src="~/plugins/jquery/jquery.min.js"></script>
<div class="card card-primary">
    <div class="card-header">
        @localizer["Transaction Monitor Case Report"]
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-sm">
                <div class="form-group">
                    <label for="dtFromDate">@localizer["From Date"]</label>
                    <input type="date" asp-for="FromDt" class="form-control" id="dtFromDate" max="@(DateTime.Now.ToString("yyyy-MM-dd"))" placeholder="From Date">
                </div>
            </div>
            <div class="col-sm">
                <div class="form-group">
                    <label for="dtToDate">@localizer["To Date"]</label>
                    <input type="date" asp-for="ToDt" class="form-control" id="dtToDate" max="@(DateTime.Now.ToString("yyyy-MM-dd"))" placeholder="To Date">
                    @*<input type="date" asp-for="ToDt" class="form-control" id="dtToDate" min="@(DateTime.Now.ToString("yyyy-MM-dd"))" max="3000-12-31" placeholder="To Date">*@
                    <span asp-validation-for="ToDt" class="text-danger"></span>
                </div>
            </div>

        </div>
    </div>
    <!-- /.card-body -->

    <div class="card-footer fc-center">
        <button type="submit" id="btnSearch" class="btn btn-primary center" onclick="LoadTable();">@localizer["Search"]</button>
    </div>
    <!--/form-->
</div>
<div class="card">
    <div class="card-header">
        <h3 class="card-title">@localizer["Search Results"]</h3>
    </div>
    <div class="card-body table-responsive p-2">
        <button type="button" onclick="ExportList(true);" class="btn btn-default mb-2" style="background-color:#f8f9fa;border-color:#ddd;">@localizer["Export to PDF"]<i class="fas fa-file-pdf"></i></button>
        <button type="button" onclick="ExportList(false);" class="btn btn-default mb-2" style="background-color:#f8f9fa;border-color:#ddd;">@localizer["Export to Excel"]<i class="fas fa-file-excel-o"></i></button>
        <table id="tmsReportTbl" class="table table-striped table-hover dt-responsive display nowrap" cellspacing="0">
            <thead>
                <tr>
                    <th>@localizer["Customer Id"]</th>
                    <th>@localizer["Customer Name"]</th>
                    <th>@localizer["Created On"]</th>
                    <th>@localizer["Rule Violated"]</th>
                    <th>@localizer["Status"]</th>
                    <th>@localizer["Action"]</th>
                </tr>
            </thead>
        </table>
    </div>
</div>

<script>
    $(document).ready(function () {
        LoadTable();
    });
    function LoadTable() {
        var fromDate = $('#dtFromDate').val();
        var toDate = $('#dtToDate').val();
        table = $('#tmsReportTbl').DataTable({
            "processing": true, // for show progress bar
            "serverSide": true, // for process server side
            "filter": true, // this is for disable filter (search box)
            "orderMulti": false, // for disable multiple column at once
            "order": [[0, 'asc']], // default sorting column(1 = "id") and sorting order
            "iDisplayLength": 25,
            "destroy": true,
            "ajax": {
                "url": "/TransactionMonitor/TMSReportCustomPagination",
                "type": "POST",
                "datatype": "json",
                "data": {
                    "fromDate": fromDate,
                    "toDate": toDate

                }
            },
            "columns": [
                { data: "customerId" },
                {
                    data: "customerName"
                },
                { data: "createdOn" },
                { data: "ruleViolatedText" },
                { data: "updatedStatus" },
                {
                    "data": "view", "name": "View", "autoWidth": true,
                    "render": function (data, type, row) {
                        return '<a class="btn btn-info" href="ViewTMSReport?id=' + row.id + '">View</a>';
                    }
                }
            ]

        });
    }
    function ExportList(isPDF) {
        $.ajax({
            type: "GET",
            url: "/TransactionMonitor/ExportTMSCaseReport/",
            data: {
                startDate: $('#dtFromDate').val(), // < note use of 'this' here
                endDate: $("#dtToDate").val(),
                IsPDF: isPDF
            },
            success: function (data) {

                window.location = '/TransactionMonitor/ExportTMSCaseReport?startDate=' + $('#dtFromDate').val() + '&endDate=' + $("#dtToDate").val() + '&IsPDF=' + isPDF;
            },
            error: function (result) {
                alert('error');
            }
        });
    }
</script>