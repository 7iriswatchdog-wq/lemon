@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer localizer
@using AML.DTO.DTO.InternalWatchListExcel;
@using AML.ViewModel.ViewModels.InternalWathcList;
@{
    ViewData["Title"] = "View Excel";
    Layout = "~/Views/Shared/AdminLTE/_Layout.cshtml";
}

@model List<InternalWatcListExcelDTO>

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="bg-surface rounded-xl border border-bordercolor shadow-none mb-4">
                    <div class="px-6 py-4 border-b border-bordercolor">
                        <h3 class="card-title" style="color:white">@localizer["List"]</h3>
                    </div>
                    <div class="p-6">
                        <div style="width: 100%; padding-left: -10px;">
                            <div class="table-responsive">
                                <table cellspacing="0" class="w-full text-sm text-left">
                                    <thead class="text-[13px] font-medium text-textlabel bg-appbg border-b-2 border-bordercolor">
                                        <tr>
                                            <th>@localizer["Customer Id"]</th>
                                            <th>@localizer["FullName"]</th>
                                            <th>@localizer["Nationality"]</th>
                                            <th>@localizer["Source"]</th>
                                            <th>@localizer["Type"]</th>
                                        </tr>
                                    </thead>
                                    @foreach (InternalWatcListExcelDTO row in Model)
                                    {
                                            <tr>
                                                <td>@row.CustomerID</td>
                                                <td>@row.FullName</td>
                                                <td>@row.Nationality</td>
                                                <td>@row.Source</td>
                                                <td>@row.Type</td>
                                            </tr>
                                    }

                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>