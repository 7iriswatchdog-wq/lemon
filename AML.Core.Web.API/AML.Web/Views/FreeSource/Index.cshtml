@{
    ViewData["Title"] = "Free Sources";
    Layout = "~/Views/Shared/AdminLTE/_Layout.cshtml";
}
@using AML.Core.Common.StaticResource

<script src="~/plugins/jquery/jquery.min.js"></script>

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Free Source Data</h3>

                    </div>
                    <div class="card-body">
                        <div class="row col-3">
                            @*<input type="button" href="@Url.Action("Upload", "FreeSource",new { Source = FreeResouce.OFAC})"
               class="btn btn-block btn-xs btn-outline-primary" value="OFAC_SDN" />
        <input type="button" disabled href="@Url.Action("Upload", "FreeSource",new { Source = FreeResouce.UN})"
               class="btn btn-block btn-xs btn-outline-primary" value="UNITED NATION" />
        <input type="button" disabled href="@Url.Action("Upload", "FreeSource",new { Source = FreeResouce.EURO})"
               class="btn btn-block btn-xs btn-outline-primary" value="EURO - EURU LIST" />
        <input type="button" disabled href="@Url.Action("Upload", "FreeSource",new { Source = FreeResouce.CONSOLIDATE})"
               class="btn btn-block btn-xs btn-outline-primary" value="CONSOLIDATE" />
        <input type="button" disabled href="@Url.Action("Upload", "FreeSource",new { Source = FreeResouce.ALL})"
               class="btn btn-block btn-xs btn-outline-primary" value="ALL - 4 SOURCES" />*@
                            <a href="#" class="btn btn-block btn-xs btn-outline-primary">OFAC_SDN</a>
                            <button href="#" disabled type="button" class="btn btn-block btn-xs btn-outline-primary">UNITED NATION</button>
                            <button href="#" disabled type="button" class="btn btn-block btn-xs btn-outline-primary">EURO - EURU LIST</button>
                            <button href=#" disabled type="button" class="btn btn-block btn-xs btn-outline-primary">CONSOLIDATE</button>
                            <button href="#" disabled type="button" class="btn btn-block btn-xs btn-outline-primary">ALL - 4 SOURCES</button>                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>



