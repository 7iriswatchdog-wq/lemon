@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer localizer
@{
    ViewData["Title"] = @localizer["Transaction Screening Case"];
    Layout = "~/Views/Shared/AdminLTE/_Layout.cshtml";
}

<script src="~/plugins/jquery/jquery.min.js"></script>
@model AML.ViewModel.ViewModels.TransactionScreening.PendingCasesRequestModel

@*<div class="se-pre-con1"></div>*@
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">


<div class="card card-primary">
    <div class="card-header">
        @localizer["Pending cases of Transaction Reference No: " + (Model.tranrefno)]
    </div>
   
    <div class="card-body">
         <input id="hdntranrefno" type="hidden" name="tranrefno" value="@(Model.tranrefno)" />

       @* <div class="row">
            <div class="col-sm-4">
                <div class="form-group">
                    <label for="dtStartDate">@localizer["Start Date"]</label>
                    <input type="date"  class="form-control" id="dtStartDate" max="@(DateTime.Now.ToString("yyyy-MM-dd"))" placeholder="Start Date">
                    <span asp-validation-for="StartDate" class="text-danger"></span>
                </div>
            </div>
            <div class="col-sm-4">
                <div class="form-group">
                    <label for="dtEndDate">@localizer["End Date"]</label>
                    <input type="date"  class="form-control" id="dtEndDate" max="@(DateTime.Now.ToString("yyyy-MM-dd"))" placeholder="End Date">
                    <span asp-validation-for="EndDate" class="text-danger"></span>
                </div>
            </div>
          
         </div>*@
            
        </div>

   @* <div class="card-footer fc-center">
        <button type="submit" id="btnSearch" class="btn btn-primary center" onclick="LoadTable();">@localizer["Go"]</button>
    </div>*@
    <!-- /.card-body -->
    <!--/form-->
</div>

                    <div class="card-body">
                        <div style="width: 100%; padding-left: -10px;">
                            <div class="table-responsive">
                                <table id="example" class="table table-striped table-hover dt-responsive display nowrap" cellspacing="0">
                                    <thead>
                                        <tr>
                                           <th>@localizer["Action"]</th>
                                            <th>@localizer["Case ID"]</th>
                                             <th>@localizer["Customer ID"]</th>
                                            <th>@localizer["Customer Name"]</th>
                                            <th>@localizer["Created On"]</th>
                                            <th>@localizer["Match Score"]</th>
                                        </tr>
                                    </thead>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>




<script type="text/javascript">
    function confirmSubmit()
    {
        var agree = confirm("Are you sure you wish to continue?");
        if (agree)
            return true;
        else
            return false;
    }
    var table;



    $(document).ready(function () {

        LoadTable();

    });


function LoadTable() {
     var tranref = $("#hdntranrefno").val();

        table = $('#example').DataTable({
            "processing": true, // for show progress bar
            "serverSide": true, // for process server side
            "filter": true, // this is for disable filter (search box)
            "orderMulti": false, // for disable multiple column at once
            "order": [[0, 'asc']], // default sorting column(1 = "id") and sorting order
            "iDisplayLength": 25,
            "destroy": true,
            ajax: {
                url: '/transactioncase/pendingcases',
                type: 'POST',
                data : function(d)
                        {
                             d.tranrefno = tranref
                        },

                complete: function (data) {
                console.log(data);
                wait = false; }
                 
            },
            columns: [
                {
                    "render": function (data, type, full, meta) { return '<a class="btn btn-info" href="/transactioncase/process/' + full.id + '">@localizer["Process"]</a>'; }
                },
                { data: "id" },
                { data: "custrefno" },
                { data: "name" },
                { data: "createdOnDB" },       
                { data: "matchscore" },
            ],
            error: function (e, message) {
                jQuery("#example").dataTable({

                }),
                    alert(message);
            },

            destroy: true
            
        });
        $.fn.dataTable.ext.errMode = 'throw';
      
    }
  
</script>